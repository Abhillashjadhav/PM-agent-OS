# Source task

Inspect PMOS decision-to-contract authoring, fixtures, and contract validation at
official main `27d0418cb258fa5e374447f88185ff1b7117f182` in an isolated
worktree. The initial packet is read-only until the shared gate design is agreed.

F-10 requires the existing handoff fixture to drive the current PEOS
`run_to_release_ready` path rather than stop at the legacy assessment. Plan
positive and seeded-negative evidence using the approved task-store artifact.
PMOS #58 already contains the approved fourteen criteria, and PEOS #203 records
`DEMONSTRATED FOR APPROVED FEATURE` under an owner-closed fallback. The
coordinating agent is retrieving those existing artifacts; do not invent another
product, amend approved expectations, or claim a new live generation.

Read applicable repository instructions. Coordinate the interface proposal with
the PEOS gate-enforcement agent, then report the smallest faithful PMOS fixture
and routing change plus a compatibility CI approach before implementation.
Do not modify approved contracts, gold, frozen receipts, previous PR worktrees,
or deployed state. No paid calls, merges, deletions, or migrations. Root handles
publishing. Allow at most two repair attempts per failed gate.

## Agreed implementation scope

The coordinating agent authorized a full current-run fixture using an explicitly
TEST-ONLY contract and receipt derived from the existing health publisher input.
Bind synthetic GATE-001 mechanically to AC-001. Preserve the historical intake
smoke separately and leave its approved contract and receipt untouched.

Prove current-run RELEASE_READY for a deterministic fixture candidate, HALTED
for a broken candidate, and fail-closed rejection of an unbound gate before
provider/execution. No external model call or synthetic owner approval claim.
The original approved task-store contract remains unsupported under the new
gate compiler; do not relabel it demonstrated or silently map its process gates
to acceptance-criterion references.
