# BAR: align stale shipped-scope references

1. **Existing path? Yes.** Correct fixture labels and an existing announcement reviewer reference; retain all installed names and directories.
2. **Approved criterion/blocker? Yes.** PRD fixtures still call Build unshipped despite `prd-first`; a positioning negative calls all Strategy unshipped; announcement-drafter calls the existing `legal-reviewer` persona unavailable.
3. **Behavior change? Bounded source correction.** Preserve each negative trigger while stating its actual ownership/scope. The reviewer can flag claims for counsel, not confer legal approval.
4. **Rejecting check first? Yes.** Commit corrected negative-trigger fixtures and the available-reviewer reference check before the announcement instruction edit. Catalogue/source checks only, not model-trigger certification.
5. **Independently revertible? Yes.** Three fixture-label changes plus the announcement fixture/instruction reference form this bounded catalogue correction.
6. **New unrequested surface? No.** No orphan deletion, installed-skill rename, persona behavior change or new positioning skill.
