---
name: decision-to-contract
description: Convert an explicitly approved PM Agent OS product decision or PRD into an ID-keyed, machine-checkable contract using the existing Production Engineering OS publisher and compiler. Use when a human decision-maker asks to hand approved product intent to Production Engineering OS or create an executable engineering contract. Do NOT use directly for unapproved ideas, unresolved product-critical questions, ordinary prose PRDs, coding, implementation, deployment, or release decisions.
---

# Decision to Contract

Produce one deterministic JSON contract plus a digest-bound approval receipt that preserves approved product intent without inventing engineering semantics.

## Ownership boundary

PM Agent OS owns the problem, hypothesis, audience, outcome metric, leading indicators, guardrails, trade-offs, scope, non-goals, stable requirement and acceptance IDs, contract-shaped acceptance intent, decision status, and accountable approver.

Production Engineering OS owns contract compilation, meaningful-RED, implementation, sandboxed execution, verification of executable coverage, evidence, and the transition to `RELEASE_READY` or `HALTED`. A human owns release.

## Verification gate

Before returning an approved executable handoff, all checks must pass:

1. The approved product-definition source has a known accountable human and no OPEN required decision; later product changes have renewed product approval.
2. Every requested behavior maps to an `FR-*`; every requirement has an explicit criterion; every `AC-*` references existing requirements.
3. Every acceptance intent maps without semantic change to exactly one admitted executable form. Actions, measures, operators, paths and trusted test/proof bindings are explicit and supported.
4. Every binary release gate has its approved `acceptance_criterion_refs`, referencing existing criteria. A description alone never counts as an executable gate.
5. The publisher's DRAFT and exact digest were shown to the accountable human; that human explicitly approved that digest before the approval publisher was called.
6. `verify_contract_approval(contract, receipt, expected_approver=...)` verifies the exact complete published contract and expected identity; the canonical loader and current pinned compiler accept it unmodified.
7. The documented receipt-bound engineering admission can accept those unchanged artifacts. Record its actual result separately from compilation; do not claim a build, release or live model run from admission.

Unavailable publisher/compiler/admission support is BLOCKED with the missing prerequisite. Never mark compilation `ACCEPTED` because the tool was unavailable. A DRAFT review request is allowed before approval; it is not an executable handoff or a substitute for any check above.

## Hard rules

1. Never infer product intent, accountable identity, approval, gate references, executable bindings or expected values.
2. Never hand-author a publisher contract or receipt digest. Use the existing canonical publisher; do not create a second serializer or approval authority.
3. Never reuse approval after changing the approved meaning or generated bytes. Return to the affected approval boundary.
4. Never relabel an unsupported product as the health example or invent a command, test, template proof or bundle interface to make it pass.
5. Keep historical fixtures immutable. Current compatibility examples and TEST-ONLY issuance are separate from owner-approved artifacts and actual delivery evidence.

## Admission gate

Before writing a contract, require all of:

- one approved product-decision artifact or approved PRD;
- `contract_status: APPROVED`; for a PRD, both `Status: Approved` and `Contract status: APPROVED` must be present and agree—any contradiction is `CONTRACT_BLOCKED: APPROVAL_STATUS_CONFLICT`;
- a non-empty accountable identity recorded as `approved_by` or PRD `Approved by`, exactly matching the human named by the user;
- no unresolved product-critical question or OPEN required publisher field;
- stable IDs for every functional requirement and acceptance criterion;
- every requirement covered by at least one explicit acceptance-intent criterion.

Normalize only these equivalent labels deterministically after the approval fields agree: PRD `Contract status` → `contract_status`, PRD `Approved by` → `approved_by`, and ordered compiler-shaped `FR-*`/`AC-*` sections → the canonical ID-bearing JSON arrays without changing their text, IDs, actions, paths, operators, or values. If a required field or executable binding is absent, return `CONTRACT_BLOCKED` with the missing fields and ask for that bounded input. Do not infer approval, product intent, actions, paths, operators, or expected values.

The source PRD's agreeing approval labels authorize conversion of that product definition. They do not mean the generated publisher contract is already approved. Reuse supplied answers and ask one remaining product question at a time. If an answer changes approved meaning, update the PRD/decision record and obtain renewed product approval before publishing its draft. Keep missing product truth OPEN separately from a complete product definition whose engineering binding is BLOCKED.

## Contract shape

Prepare one UTF-8 publisher-input JSON object. It must include the complete PM-owned product truth required by the Production Engineering OS authoring API: product name, problem, target user, desired outcome, scope, out-of-scope items, functional and non-functional requirements, executable acceptance criteria, binary gates, scored rubric, golden cases, North Star, leading metrics, guardrails, risks, and required approvals. The ID-bearing collections use canonical arrays:

```json
{
  "contract_id": "PMOS-<stable-id>",
  "contract_version": 1,
  "product_name": "<approved name>",
  "problem": "<approved problem>",
  "target_user": "<approved primary user>",
  "desired_outcome": "<approved observable outcome>",
  "scope": ["<included v1 behavior>"],
  "out_of_scope": ["<explicit exclusion>"],
  "functional_requirements": [
    {"id": "FR-001", "title": "<behavior>", "description": "<observable behavior>", "capability": "<stable capability>"}
  ],
  "acceptance_criteria": [
    {
      "id": "AC-001",
      "requirement": "FR-001",
      "criterion": "<human-readable approved proof>",
      "given": [{"path": "service.running", "operator": "eq", "value": true}],
      "when": {"action": "health", "arguments": {}},
      "then": [{"path": "result.status", "operator": "eq", "value": "ok"}]
    }
  ],
  "binary_release_gates": [{"id": "GATE-001", "description": "<binary gate>", "acceptance_criterion_refs": ["AC-001"]}],
  "scored_eval_rubric": [{"id": "RUB-001", "criterion": "<quality>", "scale": "1-5"}],
  "golden_cases": ["<representative case>"],
  "north_star_metric": "<outcome metric>",
  "leading_metrics": ["<leading measure>"],
  "guardrails": ["<must-not-worsen constraint>"],
  "non_functional_requirements": [{"id": "NFR-001", "category": "quality", "requirement": "<constraint>"}],
  "known_risks": [{"description": "<risk>", "level": "low|medium|high"}],
  "required_approvals": [{"role": "<role>", "for": "<action>"}],
  "approved_product_decisions": [{"id": "APD-001", "decision": "<explicit approved decision>"}]
}
```

The health binding above is an example, not a default for a different product. Its gate explicitly covers AC-001; preserve the user's approved coverage in a real contract. If a gate instead describes a process or policy that these criteria do not prove, leave its engineering binding BLOCKED rather than assigning unrelated criteria.

Pass this object to the existing Production Engineering OS `build_contract_draft` publisher (advertised CLI: `pmpe legacy contract draft`). Do not hand-author the DRAFT contract. The publisher adds source and approval fields, produces the exact draft digest, and reports bounded product questions when required truth is missing. Return those questions to the product-definition loop. After the human approves that exact digest, `approve_contract_draft` (advertised CLI: `pmpe legacy contract approve`) publishes the approved contract and receipt. Use the pinned interface's actual arguments and output paths; inspect its help if needed. These authoring commands do not establish a current engineering run.

Preserve additional PMOS decision context only as JSON values; never place credentials, provider commands, executable shell, or deployment instructions in the contract.

## Executable criteria

Select exactly one admitted form per criterion:

1. `given` + `when` + `then`, using only a registered action and typed paths;
2. `measure` + `operator` + `value` + positive `sample.minimum`, using only a registered measure;
3. `human_test`, bound to an existing trusted pytest file and exact node supplied for this contract;
4. `satisfied_by_template`, bound to a proof actually provided by the pinned template version.

For the frozen `barebones-1` template, the registered action is `health`. This limits that action registry, not the kinds of products PMOS may define. A different approved product can use another admitted form only when its existing trusted binding is available. If an action is unregistered, return `CONTRACT_BLOCKED: ACTION_NOT_REGISTERED`; the complete product decision can remain recorded while engineering supplies the missing supported binding. PMOS must not invent a registry extension, write the trusted evaluator, or claim a proof exists.

A proposed bundle route requires its exact supported public interface, required inputs and pinned compatibility evidence. If these are unavailable, record that engineering dependency as BLOCKED. Do not guess bundle flags or treat a legacy admission command as proof of a current build route.

Use only registered operators: `eq`, `ne`, `lt`, `lte`, `gt`, `gte`, `contains`, `not_contains`, `matches`, `is_true`, `is_false`, `is_null`, and `not_null`.

## Digest-bound contract approval

Product-decision approval authorizes conversion; it does not authorize later edits to the generated contract. Obtain the publisher's `DRAFT` with blank `approved_by` and `approved_at` and its canonical digest. Show the draft and ask the accountable human to approve that exact digest. Wait for that response. Only then use `approve_contract_draft` with that expected digest, exact approver and approval time to set `APPROVED` and create the receipt. Never infer consent from an earlier PRD approval or hand-author receipt digests.

Any edit after receipt creation invalidates approval. Delivery requires `verify_contract_approval(contract, receipt, expected_approver=...)` to return the receipt digest before compiler validation. A missing, malformed, mismatched, or stale receipt is `CONTRACT_BLOCKED: APPROVAL_RECEIPT_INVALID`.

On a publisher, receipt, loader or compiler failure, return its actual diagnostic codes and stop that handoff. Identify missing PMOS product decisions separately from unsupported engineering bindings. Never translate rejected prose by guessing or present an invented diagnostic as a compiler result.

## Output

For a DRAFT review request, return the publisher's draft path and exact digest,
the accountable approval requested, and any unresolved diagnostics. Do not issue
a receipt until that approval is received.

For an approved handoff, return:

- the approved contract path;
- the approval-receipt path and receipt digest;
- the approved contract's canonical SHA-256 digest;
- structured-criteria count;
- human-test count;
- approval identity;
- compiler result: `ACCEPTED` or `CONTRACT_BLOCKED`;
- engineering-admission result, separately from the compiler result;
- unresolved diagnostics.

## Limitations

- Compatibility is bounded by the pinned publisher/compiler and its registered actions, measures, proofs and trusted tests. An advertised authoring command does not establish an unverified bundle or build route.
- Ordinary current-authoring regressions use a separate health fixture and synthetic test-issued approval. They do not demonstrate host-agent question selection, semantic completeness, approval pauses, owner authentication or fresh product delivery.
- Structural generation instructions do not prove live-model behavioural reliability.
- One accepted contract proves boundary compatibility, not arbitrary-app generation, reusable-engine breadth, or platform adoption.
- This skill does not code, run candidates, deploy, release, or replace the accountable human decision-maker.
